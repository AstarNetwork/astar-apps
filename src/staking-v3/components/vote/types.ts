export type WizardItem = {
  title: string;
  description: string;
  number: number;
};

export type Dapp = {
  name: string;
  address: string;
  logoUrl: string;
  amount: number;
  id: number;
  mainCategory?: string;
};
